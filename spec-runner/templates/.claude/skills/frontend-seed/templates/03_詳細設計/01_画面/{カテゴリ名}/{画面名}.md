---
spec_runner:
  node_id: frontend.detail.画面.{画面名}
  kind: detailed_design
  depends_on:
    - frontend.overview.画面一覧
    - frontend.overview.画面遷移図
    - frontend.detail.コンポーネント.{コンポーネント名}  # 使用するコンポーネントの数だけ列挙
  maps_to:
    - src/app/{path}/page.tsx
    - src/components/{コンポーネント名}/  # 使用するコンポーネントの数だけ列挙
---

# {画面名}

## 概要

- **画面ID**: SCR-{番号}
- **URL**: /{path}
- **目的**: {この画面が達成すること}

## レイアウト

```
+---------------------------+
| {ヘッダー}                |
+---------------------------+
| {メインコンテンツ}         |
|                           |
+---------------------------+
| {フッター}                |
+---------------------------+
```

## 表示要素

| 要素 | 種別 | 概要 |
|------|------|------|
| {要素名} | テキスト / ボタン / フォーム / リスト | {概要} |

## 使用コンポーネント

| コンポーネント | 用途 |
|--------------|------|
| CMP-{番号} {コンポーネント名} | {用途} |

## インタラクション

| 操作 | 処理 | 遷移先 |
|------|------|--------|
| {操作} | {処理内容} | {画面名} / なし |

## API連携

| API | 呼び出しタイミング | 概要 |
|-----|-----------------|------|
| {エンドポイント} | {タイミング} | {概要} |

## バリデーション

- {バリデーションルール}

## テスト観点

- {正常系の観点}
- {エラー表示の観点}
- {権限・認証の観点}
