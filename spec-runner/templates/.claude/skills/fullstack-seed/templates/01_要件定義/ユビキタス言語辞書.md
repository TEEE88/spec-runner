---
spec_runner:
  node_id: 要件.ユビキタス言語辞書
  depends_on:
    - 要件.要件定義
  maps_to:
    - docs/03_詳細設計/01_バックエンド/01_ドメイン/
---

# ユビキタス言語辞書

```yaml
# カテゴリ名は業務に近い名前にする。複雑化した時点で「操作」「ルール・制約」「イベント」を追加する

ユーザー:
  - 用語: "{用語}"
    定義: "{ビジネス上の定義}"
    補足: "{補足}"

対象物:
  - 用語: "{用語}"
    定義: "{ビジネス上の定義}"
    補足: "{補足}"

業務概念:
  - 用語: "{用語}"
    定義: "{ビジネス上の定義}"
    補足: "{補足}"
```
