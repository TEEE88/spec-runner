---
spec_runner:
  node_id: 詳細.ドメイン.{ドメイン名}
  kind: detailed_design
  depends_on:
    - 概要.ドメインモデル
  maps_to:
    - src/domain/{ドメイン名}/
---

# {ドメイン名} ドメイン詳細設計

## 責務

- {このドメインが扱うビジネス概念}
- {このドメインの不変条件・制約}

## 集約

```mermaid
classDiagram
  class {集約ルート} {
    +{ID型} id
    +{値オブジェクト} {フィールド名}
    +{操作}() {戻り値型}
  }
  class {エンティティ} {
    +{ID型} id
  }
  class {値オブジェクト} {
    +{型} {フィールド名}
  }
  {集約ルート} "1" --> "0..*" {エンティティ} : 含む
  {集約ルート} --> {値オブジェクト} : 持つ
```

| 集約 | 責務 | ルートエンティティ |
|------|------|----------------|
| {集約名} | {責務} | {ルートエンティティ名} |

## 値オブジェクト

| 値オブジェクト | 制約・ルール |
|--------------|------------|
| {名前} | {制約} |

## ドメインルール

- {ルール1}
- {ルール2}

## テスト一覧

| ID | テスト名 | レベル | 種別 | 前提条件 | 期待結果 | 備考 |
|----|---------|--------|------|----------|----------|------|
| T-001 | {テスト名} | 単体 / 結合 / E2E | 正常系 / 異常系 / 境界値 | {前提条件} | {期待結果} | {備考} |
